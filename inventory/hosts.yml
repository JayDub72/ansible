all:
  hosts:
    ansible.home.lan:
      ansible_connection: local
  children:
    testing:
      hosts:
        server1.home.lan:
        server2.home.lan:
    mac:
      hosts:
        tycho:
    ubuntu:
      hosts:
        media.home.lan: 
        monitor.home.lan:
    monitor:
      monitor.home.lan:
      