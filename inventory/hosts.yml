all:
  hosts:
    ansible.home.lab
  children:
    testing:
      hosts:
        server1.home.lab
        server2.home.lab
    mac:
      hosts:
        tycho
    ubuntu:
      hosts:
        server1.home.lab
        server2.home.lab
